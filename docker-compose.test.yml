services:
  test_db:
    image: postgres:15
    container_name: assistantfy_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: assistantfy_test
    ports:
      - "5434:5432"